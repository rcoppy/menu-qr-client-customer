module.exports = {
  /*chainWebpack: config => {
    config.module
      .rule('sass')
      .test(/\.s(c|a)ss$/)
      .use('vue-style-loader',
        'css-loader')
      .loader('sass-loader')
      .tap(options => {
        return {
          implementation: require('sass'),
          fiber: require('fibers'),
          indentedSyntax: true,
          outputStyle: 'expanded'
        }
      })
  }*/
}